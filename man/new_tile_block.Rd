% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tile-block.R
\name{new_tile_block}
\alias{new_tile_block}
\title{Tile Block (KPI renderer)}
\usage{
new_tile_block(
  value = character(),
  group = character(),
  name = "",
  summaries = list(),
  layout = "cards",
  overline = "",
  caption = "",
  secondary = "",
  style = "plain",
  good_when = "up",
  format = "number",
  unit = "",
  drill = FALSE,
  filter_col = NULL,
  filter_value = NULL,
  ...
)
}
\arguments{
\item{value}{Character vector of numeric column name(s). Multiple columns
=> wide input (each becomes a measure).}

\item{group}{Grouping column: clusters cards / matrix rows, and the
\code{group_by} column when \code{summaries} aggregate the input in place. \code{""} = no
grouping.}

\item{name}{Column naming each KPI (long input). \code{""} = use the \code{value}
column names as measure names.}

\item{summaries}{Optional in-block aggregations: a list, each entry
\code{list(func, cols)} (\code{func} one of count / count_distinct / mean /
median / sum / min / max). Each metric becomes a card. With \code{group} set,
one cluster per group level; with \code{group} empty, grand-total cards. A
display projection only -- the block's data output stays the raw input
filtered by the click. Empty (default) = no aggregation.}

\item{layout}{\code{"cards"} or \code{"table"}. Same slots either way.}

\item{overline, caption}{Supertext / subtext. A column name reads per-cell;
any other non-empty string is a literal. \code{overline} defaults to the
measure ("Name") label and is LEGACY: it has no gear control and is not
in the registry (two pickers fed the same visual slot and read as
duplicates) -- the arg stays accepted so saved boards keep rendering.}

\item{secondary}{Precomputed reference column drawn as the secondary.
\code{""} = none.}

\item{style}{Secondary display style: \code{plain} / \code{delta} / \code{fill} / \code{pill}.}

\item{good_when}{LEGACY, ignored: polarity is always \code{"up"} (an increase
reads good). The arg stays accepted so saved boards restore; there is no
gear control and no registry entry.}

\item{format}{How the number is formatted (never a currency guess):
\code{"number"} (separators + smart decimals), \code{"compact"} (1.2M / 38.4K), or
\code{"percent"} (a fraction x100 + \%).}

\item{unit}{Free-text unit label shown next to the value / in the matrix
header (e.g. \code{"USD"}, \code{"CHF"}, \code{"apples"}). This is how you label a
currency -- the renderer never infers \code{$}.}

\item{drill}{Logical; when \code{TRUE} a card / matrix-row click emits a
categorical filter downstream. The filter column is structurally
determined (never user-picked): the \code{group} column when grouped, else
the \code{name} column on an ungrouped long KPI list. Off by
default; inert when the tile has neither (bare KPI, grand totals).}

\item{filter_col, filter_value}{Click-filter state. Kept as constructor
params so the filter round-trips through save/restore (blockr.core
deserializes a block via its constructor formals).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\value{
A transform block of class \code{tile_block}. Its \code{result} is the
upstream data filtered by the last click (passthrough when no drill),
exactly like \code{\link[=new_table_block]{new_table_block()}}.
}
\description{
The bold display of a handful of important numbers -- the KPI-card /
scorecard family. Parallel to the \code{chart} and \code{table} interactive
renderers. It maps columns to slots and draws them, in one of two layouts
of identical content (\code{cards} or \code{table}), and is opt-in clickable as a
filter (\code{drill}) using the same transport as the chart / table. Like the
table it can aggregate in place (\code{group} + \code{summaries}, the shared
aggregation vocabulary) into grand-total or per-group cards; secondaries
(delta / fill / pill) stay precomputed upstream (a tile is not a pivot).
}
\section{Input contract}{
The canonical input is a long "tile frame": one row per (group, measure)
cell. Wide input is also accepted -- when \code{value} names multiple numeric
columns each becomes a measure (measure name = column name) and the
renderer pivots to long internally (covers the one-row
\code{summarize(across())} case).
}

\section{Slots}{
\code{overline} (supertext), \code{value} (the main number, + \code{format} + \code{unit}) ,
one \code{secondary} (a precomputed reference column drawn with a \code{style}) ,
\code{caption} (subtext). The secondary \code{style} is one of \code{plain} (show the
reference), \code{delta} (up/down \% colored by sign x \code{good_when}), \code{fill} (a
progress bar to a fraction), \code{pill} (a status chip). \code{delta} / \code{target} /
\code{max} are not distinct elements -- they are one \code{secondary} + a style.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
new_tile_block()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tile_demo_data]{tile_demo_data()}}.
}
