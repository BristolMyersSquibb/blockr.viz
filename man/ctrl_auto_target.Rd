% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctrl-send.R
\name{ctrl_auto_target}
\alias{ctrl_auto_target}
\title{The board's drill destination, found by class}
\usage{
ctrl_auto_target(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{Shiny session (defaults to the current reactive domain).}
}
\value{
The block id, \code{""} when the board offers no single candidate.
}
\description{
What \code{ctrl_target = "auto"} resolves to. A sender set to \code{"auto"} is told
where to send by the board rather than by its own configuration: the one
\code{blockr.dm::new_drill_filter_block()} on the board, or -- on a board that
carries none -- the one value filter block, if there is exactly one.
}
\details{
Two or more candidates of a class resolve to nothing rather than to a
winner: with several filter blocks in play only the board author knows
which one means "the cohort the user just drilled into", so the sender
stays quiet and the gear's target picker is there to name one.
}
