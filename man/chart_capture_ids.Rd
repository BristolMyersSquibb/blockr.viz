% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-capture.R
\name{chart_capture_ids}
\alias{chart_capture_ids}
\alias{chart_capture_for}
\alias{chart_capture_request}
\alias{chart_capture_collect}
\alias{chart_capture_img}
\title{The Charts This Session Can Draw}
\usage{
chart_capture_ids(session = shiny::getDefaultReactiveDomain())

chart_capture_for(block_id, session = shiny::getDefaultReactiveDomain())

chart_capture_request(
  id,
  width,
  height,
  session = shiny::getDefaultReactiveDomain()
)

chart_capture_collect(token, session = shiny::getDefaultReactiveDomain())

chart_capture_img(x, max_width = NULL, max_height = NULL)
}
\arguments{
\item{session}{The Shiny session.}

\item{block_id}{A block id, as the board knows it.}

\item{width, height}{The box to draw into, in CSS pixels.}

\item{token}{A token from \code{chart_capture_request()}.}

\item{x}{A capture, from \code{chart_capture_collect()}.}

\item{max_width, max_height}{Optional box, in inches.}
}
\value{
\code{chart_capture_ids()} the registered keys; \code{chart_capture_for()}
the key for one block, or \code{NULL}; \code{chart_capture_request()} a token;
\code{chart_capture_collect()} \code{NULL} while the picture is in flight, and an
exhibit once it arrives.
}
\description{
The capture service: a deck asks the browser for a picture of a chart
block, at a box it chooses, and gets the bitmap back. Every chart block
registers itself while it lives, so a block whose panel was never opened
can still be drawn (offscreen, at the export's size).
}
\details{
The exchange is asynchronous, because the picture crosses the websocket:
\code{chart_capture_request()} returns a token and \code{chart_capture_collect()}
turns into an exhibit once the reply lands. An export therefore asks
first and writes the file when the pictures are in.
}
\keyword{internal}
