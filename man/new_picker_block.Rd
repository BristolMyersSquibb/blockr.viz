% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/picker-block.R
\name{new_picker_block}
\alias{new_picker_block}
\title{Picker block}
\usage{
new_picker_block(state = list(pickers = list()), ...)
}
\arguments{
\item{state}{List with \code{pickers} -- a list of picker entries, each
\code{list(into, choices, selected, multiple)}. Empty (default) auto-fills
on first data arrival with one picker over all numeric columns,
\code{into = "value"}.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A transform block of class \code{picker_block}
}
\description{
A curated column picker for locked, dashboard-style boards (the
value-selection sibling of blockr.dm's value filter block): the builder
defines one or more \emph{pickers} in the gear, the viewer operates one
select per picker and nothing else. Each picker fixes which columns are
offered (\code{choices}), which is picked (\code{selected}), and -- crucially --
the name of the \strong{output column} the pick lands in (\code{into}). Downstream
blocks map to the \code{into} names, which never change, so switching the
pick never requires reconfiguration.
}
\details{
Semantics per picker:
\itemize{
\item single (default): the picked column is \emph{copied} into \code{into}, with its
label attribute carried along (a chart's axis title follows the pick).
Copies, not renames, so two pickers may pick the same source column
(e.g. a measure against itself).
\item \code{multiple = TRUE} (at most one picker): the picked columns pivot long --
values into \code{into}, the measure identity into \verb{<into>_measure} (a
factor of column \emph{labels}, ready for facetting). Offered-but-unpicked
columns of this picker are dropped so the schema never depends on the
pick.
}

UI follows the blockr design system: \code{Blockr.Select} controls on the
block face (one per picker, labelled by \code{into}), and the picker
definitions in the gear-toggled settings band, like the value filter's
filter list.

Design records: blockr.docs design-system/target select-controls.html
(control style) and measure-switch-proposals.html (schema; the picker
generalizes the measure switch to n pickers with editable output names).
}
\examples{
if (interactive()) {
  library(blockr.core)
  serve(
    new_picker_block(
      state = list(pickers = list(
        list(into = "value", choices = c("Sepal.Length", "Sepal.Width"),
             selected = "Sepal.Length", multiple = FALSE)
      ))
    ),
    data = list(data = iris)
  )
}

}
