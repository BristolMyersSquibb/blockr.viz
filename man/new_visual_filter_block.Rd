% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visual-filter-block.R
\name{new_visual_filter_block}
\alias{new_visual_filter_block}
\title{Visual Filter Block}
\usage{
new_visual_filter_block(
  dimensions = NULL,
  measure = NULL,
  chart_types = "bar",
  ...
)
}
\arguments{
\item{dimensions}{Character vector. Which columns to show as dimension charts.
If NULL, auto-detects non-numeric columns (up to 4).}

\item{measure}{Character. Which numeric column to aggregate in the charts.
Default is the first numeric column found. User can change via dropdown.}

\item{chart_types}{Named list. Chart type per dimension ("bar", "pie", or "row").
Default is "bar" for all dimensions.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A blockr transform block that returns filtered data
}
\description{
A visual filter block using dc.js-style interactive charts. Shows multiple
bar charts for different dimensions. Clicking on a bar filters the data
to only rows matching that selection. Returns the filtered data frame.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.bi)

  # Use with demo data
  serve(
    new_visual_filter_block(),
    data = list(data = bi_demo_data())
  )
}
}
