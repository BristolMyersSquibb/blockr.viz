% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/band-series.R
\name{compute_band_series}
\alias{compute_band_series}
\title{Compute distribution-band series per group for a band chart}
\usage{
compute_band_series(
  data,
  x_col,
  y_col,
  color_by,
  series_by,
  facet_by = NULL,
  summary = "median_q1_q3",
  whiskers = "p10_p90",
  window = "adaptive",
  window_size = 45,
  min_n = 12,
  id_col = NULL,
  n_grid = 100L
)
}
\arguments{
\item{data}{Data frame.}

\item{x_col}{Numeric x column (e.g. study day).}

\item{y_col}{Numeric value column.}

\item{color_by, series_by}{Grouping columns; the band is computed per
\code{series_by} if non-NULL else \code{color_by} else over everything.}

\item{facet_by}{Facet column. Each panel holds its own cohort, so the band
is computed per facet level as well -- a facet showing one arm's panel
with every arm's band would be silently wrong. The result is keyed by
facet level first, then by series level.}

\item{summary}{Inner interval, a \code{\link[=summarize_stat]{summarize_stat()}} value.}

\item{whiskers}{Outer interval; a \code{\link[=summarize_stat]{summarize_stat()}} value or \code{"none"} to
skip it.}

\item{window}{\code{"adaptive"} (default) or \code{"fixed"}.}

\item{window_size}{Subject count (adaptive) or half-width in x units
(fixed).}

\item{min_n}{Minimum distinct subjects in a window; below it the band is
cut.}

\item{id_col}{Subject id column, counted DISTINCT for \code{n} and for the
adaptive window. \code{NULL} counts rows, which over-counts a cohort whenever
one subject contributes several observations to the same window.}

\item{n_grid}{Grid resolution across the x range.}
}
\value{
A named list or \code{NULL}.
}
\description{
The band is a box plot dragged along a continuous x: at each grid point,
take every observation inside a window around it and compute the same
statistics a box plot computes. The window is the only genuinely new idea;
\code{summary} / \code{whiskers} are the existing distribution vocabulary
(\code{\link[=summarize_stat]{summarize_stat()}}), so a band and a boxplot of the same data agree.
}
\details{
Computed in R rather than the browser, following
\code{\link[=compute_smoother_series]{compute_smoother_series()}}: the interactive chart and \code{\link[=static_chart]{static_chart()}}
then share ONE implementation, so a deck and the app cannot disagree.

Returns a named list keyed by group level (or \verb{__all__}). Each entry holds
aligned numeric vectors over the grid: \code{x}, \code{center}, \code{lo}/\code{hi} (the inner
interval, \code{summary}), \code{olo}/\code{ohi} (the outer interval, \code{whiskers}), \code{n}
(distinct subjects in the window) and \code{hw} (the window's half-width, which
varies under \code{"adaptive"} and is worth surfacing in a tooltip). Grid points
holding fewer than \code{min_n} subjects are \code{NULL} in every statistic, so the
renderer cuts the band there rather than drawing a line through thin air.

\code{out_x} / \code{out_y} / \code{out_id} carry the individual observations outside the
LOCAL Tukey fence. Those points are always classified by the Tukey rule,
whatever \code{whiskers} draws -- otherwise a \code{"p10_p90"} ribbon would flag a
fifth of the data as outliers.
}
\examples{
d <- data.frame(day = rep(1:40, 5), val = rnorm(200), id = rep(1:5, each = 40))
compute_band_series(d, "day", "val", NULL, NULL, id_col = "id",
                    window = "fixed", window_size = 5, min_n = 2)
}
\keyword{internal}
