% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary-table-block.R
\name{new_summary_table_block}
\alias{new_summary_table_block}
\title{Summary Table Block}
\usage{
new_summary_table_block(
  state = list(vars = character(), sections = character(), by = character(), stats =
    "compact", add_overall = FALSE, overall_label = "Total", indent_details = TRUE,
    nest_hierarchies = FALSE, id_var = NULL),
  ...
)
}
\arguments{
\item{state}{Initial state list. Fields:
\itemize{
\item \code{vars} — character, variables to summarise.
\item \code{sections} — character, outer section columns (0..N).
\item \code{by} — character, column-split dimensions (0..2).
\item \code{stats} — \code{"compact"} or \code{"expanded"}.
\item \code{add_overall} — logical, append an overall column.
\item \code{overall_label} — label for the overall column, default \code{"Total"}.
\item \code{id_var} — optional column name. When set, column N values and
percentages are computed over distinct values of this column
instead of row counts. Useful when each row is an event and
multiple rows can belong to the same entity (e.g. patient,
order, session).
}}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}.}
}
\description{
Blockr transform block wrapping \code{\link[=summary_table]{summary_table()}}. Multi-variable
descriptive summary where each \code{vars} entry becomes a row-section
(one row for compact numerics, six rows for expanded numerics,
one row per level for categoricals, one row per flag for logicals).
}
\details{
Output is a plain tibble with dotted section columns
(\verb{.section_1, ..., .section_k, .var, .label}), consumable by
\code{\link[=new_gt_table_block]{new_gt_table_block()}} or any renderer that understands the
convention.

The UI uses the shared \code{Blockr.Select} widget library (also used by
\code{blockr.dplyr} blocks) with a gear-icon popover for advanced options
(stats preset, overall column, indent, nest hierarchies).

Spec: \verb{blockr.design/open/table-blocks/}.
}
