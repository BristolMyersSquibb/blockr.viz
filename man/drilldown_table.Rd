% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drilldown-table-block.R
\name{drilldown_table}
\alias{drilldown_table}
\title{Drilldown Table}
\usage{
drilldown_table(
  data,
  label_col = NULL,
  value_cols = NULL,
  color = NULL,
  drill = NULL,
  elem_id = NULL,
  digits = 2L,
  max_height = "600px"
)
}
\arguments{
\item{data}{A data frame.}

\item{label_col}{Row-stub column. Defaults to the first column.}

\item{value_cols}{Columns rendered as the table body. Defaults to
every column except \code{label_col}.}

\item{color}{\code{NULL} (plain table) or a \code{\link[=drilldown_table_color]{drilldown_table_color()}}
list.}

\item{drill}{\code{NULL} (no drill-down) or a column name; clicking a
row emits a categorical filter on that column's value.}

\item{elem_id}{Shiny namespaced id used to build the \verb{_action}
input name. Required for \code{drill} to do anything.}

\item{digits}{Rounding for numeric display. Default \code{2}.}

\item{max_height}{CSS max-height of the scroll container.}

\item{title, caption}{Optional strings.}
}
\value{
An \code{\link[htmltools:tagList]{htmltools::tagList()}}.
}
\description{
Tabular sibling to the drilldown chart. Renders a plain rectangular
data frame as an interactive HTML table (sticky header, client-side
sort and search). Two optional capabilities, both off by default:
}
\details{
\itemize{
\item \strong{Coloring.} When \code{color} is a \code{\link[=drilldown_table_color]{drilldown_table_color()}} spec,
numeric cells get a value-to-background scale (diverging for
correlation matrices, sequential for heatmaps). Purely
presentational; never changes the data.
\item \strong{Drill-down.} When \code{drill} names a column, clicking a row
emits the same categorical filter message the drilldown chart
emits, so existing filter links keep working.
}
}
