% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand-groups.R
\name{expand_groups}
\alias{expand_groups}
\title{Expand a group column into one copy of each group's rows}
\usage{
expand_groups(data, col = "Group")
}
\arguments{
\item{data}{A data frame or tibble.}

\item{col}{Name of the group column.}
}
\value{
\code{data}, expanded as described above.
}
\description{
The board-wide \code{Group} column (stamped by blockr.pharma) can carry a group
definition in its \code{blockr_groups} attribute:
\verb{list(groups = <named list: group name -> raw members>, overlap = <lgl>)}.
The data keeps one row per subject; this function makes the copies a
consumer that splits by the column needs, at the last moment.
}
\details{
\itemize{
\item No \code{blockr_groups} attribute: \code{data} is returned unchanged.
\item Partition (\code{overlap = FALSE}): the column already holds group names. Rows
whose value is not a group name (\code{NA} for a dropped level) are removed,
and the column becomes a factor in group order.
\item Overlap (\code{overlap = TRUE}): the column holds the raw level. Each group
gets one copy of its member rows with the column set to the group name,
the copies are stacked in group order, and the column becomes a factor in
group order. A subject in two groups appears twice.
}

The result keeps every data-frame attribute and every attribute of the
other columns. The group column keeps its \code{blockr_source} and \code{label}, and
its \code{blockr_groups} becomes the identity partition (each name maps to
itself), so calling \code{expand_groups()} a second time changes nothing. The
definition that was applied is kept in \code{blockr_groups_expanded}, so drill
claims and colours can still map a group back to its members.

Use it only in code that splits by the column. Anything that counts across
groups (a total, an overall N) must read the unexpanded data.
}
\examples{
d <- data.frame(id = 1:4, arm = c("Pbo", "Low", "High", "High"))
d$Group <- d$arm
attr(d$Group, "blockr_source") <- "arm"
attr(d$Group, "blockr_groups") <- list(
  groups = list(All = c("Low", "High"), Pbo = "Pbo", High = "High"),
  overlap = TRUE
)
table(expand_groups(d)$Group)

}
