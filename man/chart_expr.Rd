% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-expr.R
\name{chart_expr}
\alias{chart_expr}
\alias{chart_code}
\title{Emit a Chart Block's State as Plain ggplot2 Code}
\usage{
chart_expr(
  var = "data",
  chart_type = "bar",
  group = NULL,
  color = NULL,
  facet = NULL,
  value = ".count",
  func = "count",
  x = NULL,
  y = NULL,
  series = NULL,
  bar_mode = "stacked",
  orientation = NULL,
  sort_by = NULL,
  sort_dir = NULL,
  count_on = "off",
  count_col = NULL,
  facet_scales = "fixed",
  box_points = "none",
  smoother = "none",
  identity_line = FALSE,
  lo = NULL,
  hi = NULL,
  step = NULL,
  vlines = NULL,
  hlines = NULL,
  line_width_mult = 1,
  dot_size_mult = 1,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  data = NULL,
  scale_map = NULL,
  qualify = FALSE,
  ...
)

chart_code(expr, width = 76L)
}
\arguments{
\item{var}{The variable name (string) holding the chart's data in the
emitted code.}

\item{chart_type, group, color, facet, value, func, x, y, series}{Chart state,
as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{bar_mode, orientation, sort_by, sort_dir}{Bar layout and category
ordering, as in \code{\link[=new_chart_block]{new_chart_block()}}. Unset values resolve per type like
the canvas: a bar lies horizontal and sorts by value, descending; a
boxplot stands vertical and keeps the data's own order, ascending.}

\item{count_on, count_col}{Observation-count labels, as in
\code{\link[=new_chart_block]{new_chart_block()}}.}

\item{facet_scales}{Panel scales, passed straight to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}'s \code{scales}: \code{"fixed"} (default), \code{"free_y"},
\code{"free_x"} or \code{"free"}. Same argument as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{box_points, smoother, identity_line, lo, hi}{Family-specific options,
as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{step}{Deprecated spelling of the step modes (\code{"start"} /
\code{"middle"} / \code{"end"}); folded into \code{connect} when that is unset.}

\item{vlines, hlines}{Numeric helper-line positions.}

\item{line_width_mult, dot_size_mult}{Size multipliers.}

\item{title, subtitle, caption}{Title band text; \code{{token}} templates
resolve against \code{data} (same resolver as the app), \code{NULL} falls back
to the data's display attributes (\code{label} / \code{subtitle} / \code{caption},
the canvas auto tier), \code{""} suppresses.}

\item{data}{Optional data snapshot (the block's result). When supplied,
data-dependent decisions are baked into the emitted code as literals;
when \code{NULL}, the code recomputes them at run time.}

\item{scale_map}{A blockr.theme scale map binding columns to colors.
Default \code{NULL} reads the board's \code{scale_map} option when called inside
a live session (the officer deck path) and falls back to palette
cycling headless (the quarto path).}

\item{qualify}{Prefix every non-base function with its namespace
(\verb{ggplot2::}, \verb{dplyr::}, \verb{stats::})? Qualified code evaluates anywhere
without \code{library()} calls -- the form a generated document wants.
Unqualified code (the default) is the form a human wants to read, and
assumes \code{library(ggplot2)} + \code{library(dplyr)}.}

\item{...}{Ignored; accepted so emitted calls stay forward-compatible.}

\item{expr}{An expression as returned by \code{chart_expr()}.}

\item{width}{Target line width for the formatted code.}
}
\value{
A quoted expression (a single call), or \code{NULL} when the state
cannot be compiled. Use \code{\link[=chart_code]{chart_code()}} for a formatted character
rendering.

\code{chart_code()}: the expression formatted as a single string, one
pipeline stage / ggplot layer per line.
}
\description{
The expression-emitting sibling of \code{\link[=static_chart]{static_chart()}}. Where \code{static_chart()}
\emph{interprets} chart-block state at render time (and therefore ships with
every rendered document), \code{chart_expr()} \emph{compiles} the same state into a
plain dplyr + ggplot2 pipeline -- the code an analyst would have written by
hand. A document carrying that code reproduces the chart with no blockr
dependency at all.
}
\details{
The emitted program is a single expression of the shape

\if{html}{\out{<div class="sourceCode r">}}\preformatted{data |>
  count(SEX, ARM, name = "n") |>
  ggplot(aes(x = n, y = reorder(SEX, n, sum), fill = ARM)) +
  geom_col(width = 0.6) +
  scale_fill_manual(values = c(...)) +
  labs(title = "Count by SEX and ARM", x = "Count", y = NULL) +
  theme_minimal(base_size = 11) +
  theme(...)
}\if{html}{\out{</div>}}

Two compilation modes exist, keyed on \code{data}:
\itemize{
\item \strong{With \code{data}} (a snapshot of the block's result), data-dependent
choices are \emph{baked in as literals}: level colors resolve through the
board scale map / house palette into a named \code{c(level = "#hex")} vector,
\code{{token}} title templates resolve to their text, count labels become a
named label vector, and the line chart's marker cutoff is decided. This
is the reproducible-report mode: the document freezes what you saw.
\item \strong{Without \code{data}}, the emitted code \emph{recomputes} those choices at run
time (label functions over the data variable, unnamed palette vector,
templates left verbatim). The code stays correct under new data but is
slightly less pretty.
}

Argument names mirror \code{\link[=new_chart_block]{new_chart_block()}} / \code{\link[=static_chart]{static_chart()}} one to one.
Supported types are the \code{\link[=static_chart]{static_chart()}} set: \code{"bar"} (stacked / grouped /
percent, \code{func = "identity"} for precomputed heights), \code{"boxplot"},
\code{"scatter"} and \code{"line"}. An uncovered type -- or covered-but-undrawable
state (no group on a bar, say) -- returns \code{NULL}, so callers can fall back
to the \code{static_chart()} call form.
}
\examples{
ex <- chart_expr("aes_data", chart_type = "bar", group = "Species")
cat(chart_code(ex))

}
