% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kpi-block.R
\name{new_kpi_block}
\alias{new_kpi_block}
\alias{block_ui.kpi_block}
\alias{block_render_trigger.kpi_block}
\alias{block_output.kpi_block}
\title{KPI Block}
\usage{
new_kpi_block(
  measures = character(),
  agg_fun = "sum",
  prefix = "",
  suffix = "",
  digits = "0",
  titles = NULL,
  subtitles = NULL,
  colors = NULL,
  ...
)

\method{block_ui}{kpi_block}(id, x, ...)

\method{block_render_trigger}{kpi_block}(x, session = shiny::getDefaultReactiveDomain())

\method{block_output}{kpi_block}(x, result, session)
}
\arguments{
\item{measures}{Character vector. The numeric columns to aggregate.}

\item{agg_fun}{Character. Aggregation function: "sum", "mean", "median", "min", "max", "n".}

\item{prefix}{Character. Text before each number (e.g., "$").}

\item{suffix}{Character. Text after each number (e.g., "\%", "M").}

\item{digits}{Integer. Decimal places for rounding. Default 0.}

\item{titles}{Named character vector. Custom titles for the colored pill labels.
Names should match measure names. If NULL or missing for a measure, uses the
measure name as title.}

\item{subtitles}{Named character vector. Optional subtitles shown below each value.
Names should match measure names. If NULL or missing for a measure, no subtitle shown.}

\item{colors}{Named character vector. Custom colors for each measure's pill.
Names should match measure names. Values should be hex colors.
If NULL or missing for a measure, auto-assigns from palette.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}

\item{session}{Shiny session object}
}
\value{
A blockr transform block that displays KPIs
}
\description{
A block for displaying one or more key performance indicators (KPIs) as
prominent numbers. Useful for dashboard headlines like "Revenue: $67M".
Select multiple measures to display them side by side with auto-assigned colors.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.bi)
  library(blockr.dag)

  # Multiple KPIs in one block
  serve(
    new_kpi_block(
      measures = c("Revenue", "Profit", "Transactions"),
      prefix = "$"
    ),
    data = list(data = bi_demo_data())
  )

  # Simple dashboard
  run_app(
    blocks = c(
      data = new_static_block(bi_demo_data()),
      kpis = new_kpi_block(measures = c("Revenue", "Profit", "Transactions"))
    ),
    links = c(
      new_link("data", "kpis", "data")
    ),
    extensions = list(new_dag_extension())
  )
}
}
