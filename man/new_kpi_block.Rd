% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tile-block-deprecated.R
\name{new_kpi_block}
\alias{new_kpi_block}
\title{Deprecated: KPI block}
\usage{
new_kpi_block(
  measures = character(),
  agg_fun = "sum",
  prefix = "",
  suffix = "",
  digits = 0,
  titles = NULL,
  subtitles = NULL,
  colors = NULL,
  ...
)
}
\arguments{
\item{measures}{Character vector of columns to display as KPIs.}

\item{agg_fun}{Aggregation function: one of \code{"sum"}, \code{"mean"},
\code{"median"}, \code{"min"}, \code{"max"}, \code{"n"} (count). Mapped to the new
block's \code{stats$value}.}

\item{prefix, suffix}{Glyphs for currency / percent. Converted to
a format kind when they match known symbols (\code{"$"}, \code{"\%"},
\code{"€"}, \code{"£"}).}

\item{digits}{Integer, decimals to display.}

\item{titles, subtitles, colors}{Silently dropped — see details.}

\item{...}{Forwarded to \code{\link[=new_tile_block]{new_tile_block()}}.}
}
\value{
A tile block with \code{showcase = "number"}.
}
\description{
\code{new_kpi_block()} is superseded by \code{\link[=new_tile_block]{new_tile_block()}}. The new
block separates aggregation (per-aesthetic stat) from rendering
and ships three visual modes (number / spark / progress).
}
\details{
Calls to \code{new_kpi_block()} still work: their arguments are
converted to an equivalent \code{new_tile_block(showcase = "number")}
state. \code{titles}, \code{subtitles}, and \code{colors} are dropped — their
role is now data-driven via the \code{label} / \code{status} aesthetics.
Bake any per-measure label / color overrides into a \code{mutate()}
upstream and map them to \code{label} / \code{status}.
}
\keyword{internal}
