% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/band-series.R
\name{band_half_width}
\alias{band_half_width}
\title{Half-width of the window centred on one grid point}
\usage{
band_half_width(dist, id_code, n_ids, window, window_size, max_half)
}
\arguments{
\item{dist}{Absolute distances from the grid point to every observation.}

\item{id_code}{Integer subject codes aligned with \code{dist} (\code{match()} against
the level set), or \code{NULL} to count rows. Integer codes and a
pre-allocated seen-vector, not \code{unique()} on a growing character vector:
the latter is quadratic in the window and this runs once per grid point.}

\item{n_ids}{Number of distinct subject codes.}

\item{window}{\code{"fixed"} or \code{"adaptive"}.}

\item{window_size}{Half-width in x units (fixed) or subject count
(adaptive).}

\item{max_half}{Cap on the adaptive half-width.}
}
\value{
A single numeric half-width.
}
\description{
\code{"fixed"} is \code{window_size} in x units. \code{"adaptive"} grows the window until
it holds \code{window_size} distinct ids, which keeps the band's stability
constant as the cohort thins -- the fixed window is simple to explain but
goes empty wherever the design is sparse (scheduled visits 56 days apart
late in a study leave a narrow window sitting in the gap between them).
}
\keyword{internal}
