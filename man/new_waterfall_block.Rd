% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall-block.R
\name{new_waterfall_block}
\alias{new_waterfall_block}
\alias{block_ui.waterfall_block}
\alias{block_output.waterfall_block}
\title{Waterfall Block}
\usage{
new_waterfall_block(
  measures = character(),
  colors = list(increase = "#22c55e", decrease = "#ef4444", total = "#3b82f6"),
  ...
)

\method{block_ui}{waterfall_block}(id, x, ...)

\method{block_output}{waterfall_block}(x, result, session)
}
\arguments{
\item{measures}{Character vector. The numeric columns representing steps in
the waterfall, in order. Each subsequent measure should include the previous.}

\item{colors}{Named list. Colors for increase/decrease/total bars.
Default: increase = "#22c55e" (green), decrease = "#ef4444" (red), total = "#3b82f6" (blue).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}

\item{id}{Module ID (for S3 methods)}

\item{x}{Block object (for S3 methods)}

\item{result}{Evaluation result (for S3 methods)}

\item{session}{Shiny session object (for S3 methods)}
}
\value{
A blockr transform block that displays a waterfall chart
}
\description{
A waterfall/bridge chart block for visualizing sequential value progression.
Shows how a value builds from one measure to another (e.g., Revenue -> Costs ->
Profit, or Q1 -> Q2 -> Q3 -> Q4).
}
\details{
Common use cases include P&L statements, budget variance analysis, revenue
bridges, and any sequential measure comparison.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.bi)

  # P&L waterfall example
  pnl_data <- data.frame(
    Revenue = 1000000,
    Gross_Profit = 600000,
    Operating_Income = 400000,
    Net_Income = 300000
  )

  serve(
    new_waterfall_block(
      measures = c("Revenue", "Gross_Profit", "Operating_Income", "Net_Income")
    ),
    data = list(data = pnl_data)
  )
}
}
