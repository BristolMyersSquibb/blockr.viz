% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe-chart.R
\name{describe_block.chart_block}
\alias{describe_block.chart_block}
\title{Describe a chart block for the assistant}
\usage{
\method{describe_block}{chart_block}(x, board, id, ..., state = NULL)
}
\arguments{
\item{x}{A \code{chart_block}.}

\item{board}{Board snapshot, for link metadata.}

\item{id}{Block id on the board.}

\item{...}{Passed on.}

\item{state}{Live block state, or \code{NULL}. Argument order follows the
generic, which takes it after the dots, so it is always named.}
}
\value{
Character vector of lines.
}
\description{
Reports what the chart is currently plotting rather than the constructor
argument dump the default method produces: the chart type, the column
mappings, the aggregation, and whether a drill filter is narrowing the data
it passes downstream.
}
\details{
Falls back to the default method when no live state is supplied, which
happens for a block that was never built, or against a \code{blockr.assistant}
that does not yet pass state.
}
