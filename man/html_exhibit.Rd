% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-exhibit.R
\name{html_exhibit}
\alias{html_exhibit}
\title{Static HTML Renderer for a Display Table}
\usage{
html_exhibit(x, title = NULL, caption = NULL, max_height = NULL, ...)
}
\arguments{
\item{x}{A data frame or \code{\link[=as_annotated_df]{as_annotated_df()}}-coercible table object.}

\item{title, caption}{Table title / footnote. \code{NULL} (default) takes them
from the annotated data frame's display attributes, \code{""} switches them
off -- the same title-tier convention as \code{\link[=static_table]{static_table()}}.}

\item{max_height}{CSS max-height of the scroll container, or \code{NULL}
(default) for none. A report exhibit is printed rather than scrolled.}

\item{...}{Passed to the underlying renderer.}
}
\value{
A \code{\link[htmltools:tagList]{htmltools::tagList()}}.
}
\description{
The HTML counterpart of \code{\link[=static_table]{static_table()}}: the same annotated data frame,
rendered as the hand-rolled HTML display table the app itself draws, with
its styles and its collapse / search script inlined. Nothing in the
output talks to Shiny, so the result is a self-contained exhibit that
survives being written to a standalone HTML file.
}
\details{
Called by \code{\link[=static_exhibit]{static_exhibit()}} when the render target is HTML; exported so a
document can ask for it explicitly.
}
\seealso{
\code{\link[=static_exhibit]{static_exhibit()}}, \code{\link[=static_table]{static_table()}}
}
