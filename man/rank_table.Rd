% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank-table-html.R
\name{rank_table}
\alias{rank_table}
\title{Ranked bar table}
\usage{
rank_table(
  data,
  group = NULL,
  value = ".count",
  func = "count",
  id_var = NULL,
  summaries = list(),
  by = NULL,
  facet_layout = "by_summary",
  parent = NULL,
  color = NULL,
  bar_mode = "stacked",
  facet = NULL,
  cols = NULL,
  fields = NULL,
  sort_by = "value",
  sort_dir = "desc",
  top_n = NULL,
  max_height = "600px",
  search = TRUE,
  sortable = TRUE,
  axis = TRUE,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  drill = NULL,
  scale_map = NULL,
  elem_id = NULL,
  active = NULL,
  expanded = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{group}{Column to rank by (one row per level).}

\item{value, func, id_var}{The measure. \code{func} is one of \code{"count"},
\code{"count_distinct"}, \code{"sum"}, \code{"mean"}, \code{"median"}, \code{"min"}, \code{"max"};
\code{value} names the column it reduces (ignored by \code{"count"}), \code{id_var} the
subject identifier \code{"count_distinct"} counts.}

\item{summaries, by, facet_layout}{The summarize-table path: \code{summaries} is
an ordered list of typed summary rows (see
\code{\link[=new_summarize_table_block]{new_summarize_table_block()}}); non-empty, it takes over from the flat
bar mappings. \code{by} is the grouping vector (outer -> inner, at most
two).}

\item{parent}{Optional outer grouping column: parents become expandable
rows with their children indented under them. A parent is aggregated in
its own pass, never summed from its children.}

\item{color}{Optional column splitting each bar into segments. Composes
with \code{facet}.}

\item{bar_mode}{\code{"stacked"}, \code{"grouped"} or \code{"percent"}. No-op without
\code{color}.}

\item{facet}{Optional column giving one bar column per level, all on one
shared scale.}

\item{cols}{Opt-in separate numeric columns beside the bar: any of \code{"n"},
\code{"pct"}. By default the bar cell carries its own value label.}

\item{fields}{Extra columns from the underlying row (identity measure
only), shown as real columns beside the bar.}

\item{sort_by, sort_dir}{Server-side ordering. \code{sort_by} is \code{"value"},
\code{"data"} (the data's own order), \code{"label"}, a summary column name or a
facet level name; \code{sort_dir} is \code{"desc"} or \code{"asc"}.}

\item{top_n}{Optional cap. Off by default -- the table scrolls instead.
When set, the rows below the cut are reported in a visible fold row.}

\item{max_height}{CSS max-height of the scroll container.}

\item{search}{Show the search input.}

\item{sortable}{Allow click-to-sort on the column headers.}

\item{axis}{Print each glyph column's domain as a tick strip under its
header (default \code{TRUE}): the value domain for bars, boxes and dot
ranges, the x domain (dates as dates) for swimlanes and sparklines.}

\item{title, subtitle, caption}{Display text, already resolved (see
\code{resolve_block_title()}).}

\item{drill}{Column a row click filters on, or \code{NULL} for a display-only
table.}

\item{scale_map}{Board scale map, for palette agreement with the charts.}

\item{elem_id}{Shiny namespaced id used to build the \verb{_action} input the
JS emits. \code{NULL} outside a block (a static table).}

\item{active}{Active drill filter, \code{list(col =, vals =)}, for the row
highlight and the status line.}

\item{expanded}{Open every nested row. \code{FALSE} (default) is the dashboard's
collapsed-to-parents view; an EXPORT sets it, having nobody to click the
chevron.}
}
\value{
An \code{\link[htmltools:tagList]{htmltools::tagList()}}.
}
\description{
Renders a ranked horizontal-bar table: one row per level of \code{group},
ordered by the measure, with the bar drawn as a div in a cell. Search,
click-to-sort, exact values, a sticky header and an arbitrary row count
come from the table form; the bar carries the magnitude.
}
\details{
The bar column takes one of three shapes, picked by the arguments:
plain (\code{group} only), split into segments (\code{color}), or one column per
level (\code{facet}).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
rank_table(mtcars, group = "cyl", func = "count")
\dontshow{\}) # examplesIf}
}
