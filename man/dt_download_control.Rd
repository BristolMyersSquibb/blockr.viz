% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-download.R
\name{dt_download_control}
\alias{dt_download_control}
\title{Install a table's download control}
\usage{
dt_download_control(
  session,
  exhibit,
  enabled = NULL,
  slot_id = "dt_download",
  filename = "table"
)
}
\arguments{
\item{session}{The calling module's Shiny session. Handlers are registered
on its \code{output}, so the caller must own the namespace.}

\item{exhibit}{A function of no arguments returning the list the writers
are called with: \code{data} (the annotated frame), \code{title}, \code{subtitle},
\code{caption}, and the \code{collapsible} / \code{sortable} display toggles the HTML
page carries over. Read at click time, so a stale snapshot is never
written.}

\item{enabled}{A function of no arguments returning \code{TRUE} when downloads
are on. \code{NULL} (the default) means always on, which is the answer for a
caller with no gear to switch them off in.}

\item{slot_id}{Output id for the control itself.}

\item{filename}{Base name for the written file, without extension.}
}
\value{
A \code{\link[shiny:htmlOutput]{shiny::uiOutput()}} to place on the toolbar.
}
\description{
Registers the download handlers for every format this machine can write and
returns the slot to drop on the table's toolbar (\code{dt_chrome()}'s
\code{download_slot}, which \code{\link[=drilldown_table]{drilldown_table()}} takes as \code{download_slot}). Each
format writes the SAME frame the table shows, through that format's own
writer: \code{\link[=write_annotated_xlsx]{write_annotated_xlsx()}} for the spreadsheet,
\code{\link[=write_exhibit_html]{write_exhibit_html()}} for a self-contained page, \code{\link[=write_exhibit_pptx]{write_exhibit_pptx()}}
for a deck. That last pair is the exhibit machinery blockr.outline's report
and deck exports use, so a table downloaded here and the same table in a
deck are one artifact.
}
\details{
Registration is guarded per session and slot, because a caller may run on
every re-render: \code{block_result_output()} does, once per evaluation of its
block, and re-registering a handler each time would leak one output per
render.
}
