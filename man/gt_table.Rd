% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt-table-block.R
\name{gt_table}
\alias{gt_table}
\title{gt Table Renderer for the Table-Blocks Quartet}
\usage{
gt_table(
  data,
  title = NULL,
  subtitle = NULL,
  full_width = TRUE,
  borders = TRUE,
  na_rep = "—"
)
}
\arguments{
\item{data}{A data frame (or an object with an \code{\link[=as_annotated_df]{as_annotated_df()}}
method, e.g. a composer table -- coerced on entry). Either:
\itemize{
\item \strong{Annotated / wide format}: a \code{\link[=summary_table]{summary_table()}}-style frame with
optional dotted columns (\verb{.section_*}, \code{.label}, \code{.indent},
\code{.strong}, \code{.emph}) and data cells (pipe-delimited if nested), or
the tidy \code{.fmt} form (numbers + per-row template), which is spread
to the wide grid via \code{fmt_to_wide()}. A plain frame with none of
the dotted columns renders as a plain gt table.
\item \strong{Legacy long format}: has \code{label}, \code{depth}, \code{col_var} columns
plus stat columns (\code{n}/\code{N}/\code{pct} or \code{mean}/\code{sd} or \code{value}).
Deprecated -- shape with \code{\link[=summary_table]{summary_table()}} instead.
}}

\item{title}{Optional table title.}

\item{subtitle}{Optional subtitle shown under the title.}

\item{full_width}{Logical. If \code{TRUE} (default) the table stretches
to 100\% width. Set \code{FALSE} for auto-sized.}

\item{borders}{Logical. If \code{TRUE} (default) a 2px solid border
brackets the table top and bottom, and the title area gets a
2px bottom border.}

\item{na_rep}{Character. Text to display for missing (\code{NA}) cells.
Default \code{"\\u2014"} (em dash), the clinical-table convention. Use
\code{""} for blank, \code{"NA"} for gt's built-in default, or any other
string.}
}
\value{
A \code{gt_tbl} object.
}
\description{
Renders the output of \code{\link[=summary_table]{summary_table()}} (or any block following the
"wide tibble with dotted section columns" convention) as a styled gt
table. Pivoted display grids are produced upstream by composing
\code{summarize} with \code{tidyr::pivot_wider}, not by a dedicated block.
Also supports the legacy
long-format input from \code{blockr.sandbox}'s \code{tidy_summary_block} and
\code{occurrence_summary_block} via a separate code path.
}
\details{
Column headers are driven by \code{attr(col, "label")} -- a standard R
convention. \code{gt::gt()} reads these labels natively. If a column
has no label attribute, the column name is used. Upstream blocks
(including \code{\link[=summary_table]{summary_table()}}) can set labels on the columns they
produce, and the renderer picks them up automatically. Users who
want custom labels can use a \code{mutate_block} upstream to overwrite
\code{attr(col, "label")}.
}
\examples{
tbl <- summary_table(iris, vars = "Sepal.Length", by = "Species")
gt_table(tbl, title = "Sepal length by species")
}
