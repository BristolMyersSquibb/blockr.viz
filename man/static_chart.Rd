% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-chart.R
\name{static_chart}
\alias{static_chart}
\title{Static ggplot Renderer for Chart-Block State}
\usage{
static_chart(
  data,
  chart_type = "bar",
  group = NULL,
  color = NULL,
  facet = NULL,
  value = ".count",
  func = "count",
  x = NULL,
  y = NULL,
  series = NULL,
  bar_mode = "stacked",
  orientation = "horizontal",
  sort_by = "value",
  sort_dir = NULL,
  count_on = "off",
  count_col = NULL,
  facet_scales = "fixed",
  box_points = "none",
  smoother = "none",
  identity_line = FALSE,
  lo = NULL,
  hi = NULL,
  step = NULL,
  vlines = NULL,
  hlines = NULL,
  line_width_mult = 1,
  dot_size_mult = 1,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  scale_map = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame (the block's result: its filtered input).}

\item{chart_type, group, color, facet, value, func, x, y, series}{Chart state,
as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{bar_mode, orientation, sort_by, sort_dir}{Bar layout and category
ordering, as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{count_on, count_col}{Observation-count labels, as in
\code{\link[=new_chart_block]{new_chart_block()}}.}

\item{facet_scales}{Panel scales, passed straight to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}'s \code{scales}: \code{"fixed"} (default), \code{"free_y"},
\code{"free_x"} or \code{"free"}. Same argument as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{box_points, smoother, identity_line, lo, hi, step}{Family-specific
options, as in \code{\link[=new_chart_block]{new_chart_block()}}.}

\item{vlines, hlines}{Numeric helper-line positions.}

\item{line_width_mult, dot_size_mult}{Size multipliers.}

\item{title, subtitle, caption}{Title band text; \code{{token}} templates
resolve against \code{data} (same resolver as the app), \code{NULL} composes an
automatic title, \code{""} suppresses.}

\item{scale_map}{A blockr.theme scale map binding columns to colors.
Default \code{NULL} reads the board's \code{scale_map} option when called inside
a live session (the officer deck path) and falls back to palette
cycling headless (the quarto path).}

\item{...}{Ignored; accepted so emitted calls stay forward-compatible.}
}
\value{
A ggplot object, or (for unsupported types) the aggregated data
frame the chart would have drawn.
}
\description{
Renders the chart block's configuration as a ggplot -- the static chart
output for report / deck rendering, the plot-side sibling of
\code{\link[=static_table]{static_table()}}. The interactive chart draws client-side (canvas); a
rendered document has no browser, so blockr.outline emits
\verb{static_chart(<result>, <state...>)} for chart blocks and this function
rebuilds the chart server-side from the same vocabulary.
}
\details{
Numbers match the app by construction: aggregation goes through the same
engine the table/tile renderers use (\code{dd_table_aggregate}, golden-tested
against the JS engine), colors cycle the same \code{dd_palette()} with
the same level ordering (factor levels, else sorted unique), and a board
scale map (arm colors) resolves through the same blockr.theme resolver.
The look is print typography, not a pixel copy of the canvas.

Argument names mirror \code{\link[=new_chart_block]{new_chart_block()}} one to one, so the emitted call
is literally the block's state. Interaction-only state (tooltips, drill,
zoom, runtime filters) has no meaning in print and is not part of this
surface.

Supported types: \code{"bar"} (stacked / grouped / percent, \code{func = "identity"} for precomputed heights), \code{"boxplot"}, \code{"scatter"} and
\code{"line"}. Any other type degrades to the aggregated data (a table in the
rendered document) with a warning, so a report never breaks on an
uncovered chart type.

For pptx decks the returned plot carries \code{pptx_width} / \code{pptx_height}
attributes sized from the chart's fixed row geometry (28px per category
row, 14px per series in grouped mode -- the canvas constants), so
officer places each chart at its natural height instead of a one-size
box.
}
