% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure-switch-block.R
\name{new_measure_switch_block}
\alias{new_measure_switch_block}
\title{Measure switch block}
\usage{
new_measure_switch_block(
  choices = character(),
  selected = character(),
  multiple = FALSE,
  ...
)
}
\arguments{
\item{choices}{Character vector of columns offered to the viewer. Empty
(default) auto-fills with all numeric columns on first data arrival.}

\item{selected}{Character vector of currently picked column(s). Empty
defaults to the first choice.}

\item{multiple}{Allow picking several measures at once (viewer select
switches to tag mode; downstream typically facets by \code{measure}).}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A transform block of class \code{measure_switch_block}
}
\description{
A curated measure picker for locked boards: the builder fixes which
columns are offered (\code{choices}), the viewer picks one (or several, with
\code{multiple = TRUE}). The block always emits the long shape -- a \code{measure}
column (factor of column \emph{labels}) plus a \code{value} column -- even for a
single pick, so a downstream chart mapped to \code{value} (and optionally
facetted by \code{measure}) never sees a schema change when the selection
grows or shrinks. Offered-but-unpicked measures are dropped for the same
reason.
}
\details{
UI follows the blockr design system: the viewer control is a
\code{Blockr.Select} (single mode, or multi with tags when \code{multiple}), and
the builder settings (measures offered, allow multiple) live in the
gear-toggled in-flow settings band, exactly like blockr.dplyr's
pivot-longer block. An empty selection is never committed: the control
snaps back to the last non-empty pick.

Design record: blockr.docs/design-system/target/measure-switch-proposals.html
(control style superseded by select-controls.html -- blockr-select
everywhere).
}
