% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctrl-send.R
\name{ctrl_targets}
\alias{ctrl_targets}
\title{Controllable blocks on the board}
\usage{
ctrl_targets(
  class = "value_filter_block",
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{class}{Character(1). Block class to look for. The default is the value
filter block, which is the one target shape \code{\link[=ctrl_send]{ctrl_send()}} senders in this
package push (a \code{state} of filter conditions); it serves both plain data
frames and \code{dm}s.}

\item{session}{Shiny session (defaults to the current reactive domain).}
}
\value{
A named character vector of block ids, named by block name (falling
back to the id). Empty when there is no channel or no candidate.
}
\description{
The block ids a sender may point at: every block on the board of the given
class. Populates the target picker of the drill senders (the table, chart
and tile blocks' external-control option), and is available to a hand-rolled
sender (a function block calling \code{\link[=ctrl_send]{ctrl_send()}}) that wants one too.
}
\details{
Reads the board reactively, so a picker built on it re-renders as blocks are
added and removed. Returns \code{character()} when no channel is installed, or
when \code{\link[=install_ctrl_send]{install_ctrl_send()}} was called without \code{board} -- in which case a
sender should fall back to a plain block-id field rather than offering an
empty picker.
}
