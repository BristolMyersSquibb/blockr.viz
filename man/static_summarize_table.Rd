% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize-exhibit.R
\name{static_summarize_table}
\alias{static_summarize_table}
\title{A Summarize Table as a Printable Exhibit}
\usage{
static_summarize_table(data, ...)
}
\arguments{
\item{data}{A data frame (or \code{\link[=as_annotated_df]{as_annotated_df()}}-coercible object), the
table's input.}

\item{...}{Passed to \code{\link[=rank_table]{rank_table()}}: the block's own vocabulary (\code{by},
\code{summaries}, \code{group}, \code{color}, \code{facet}, \code{sort_by}, \code{top_n}, \code{title}, ...).}
}
\value{
An object of class \code{summarize_exhibit}.
}
\description{
The summarize table (\code{new_summarize_table_block()}) rebuilt outside Shiny,
as an object a report, a deck or a download can render. The block draws the
table by pushing a cell model to the browser, so its result is only the
data; this is what puts the table itself into a document.
}
\details{
The returned object renders per target and never re-derives the marks:
\code{\link[=html_exhibit]{html_exhibit()}} gives the same HTML table the app draws, and
\code{\link[=pptx_add_exhibit]{pptx_add_exhibit()}} paints it and places it on as many slides as it needs.
Both read one cell model, computed here.

On a slide the exhibit is a PICTURE, because PowerPoint's table cells hold
text runs only and a distribution glyph cannot live in one. The text is
therefore not selectable or editable on those slides. A plain display table
has no such constraint and keeps \code{\link[=static_table]{static_table()}}.
}
\examples{
\dontshow{if (requireNamespace("systemfonts", quietly = TRUE)) withAutoprint(\{ # examplesIf}
ex <- static_summarize_table(
  mtcars, by = "cyl",
  summaries = list(
    list(type = "simple", func = "count", show = "bar", name = "Cars"),
    list(type = "dist", col = "mpg", style = "box",
         inner = "median_q1_q3", outer = "p10_p90", name = "MPG")
  )
)
ex
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=rank_table]{rank_table()}}, \code{\link[=pptx_add_exhibit]{pptx_add_exhibit()}}, \code{\link[=html_exhibit]{html_exhibit()}}
}
