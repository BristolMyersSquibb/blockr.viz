% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivot-table-block.R
\name{new_pivot_table_block}
\alias{new_pivot_table_block}
\title{Pivot Table Block}
\usage{
new_pivot_table_block(
  rows = character(),
  cols = character(),
  measures = character(),
  agg_fun = "sum",
  digits = "",
  ...
)
}
\arguments{
\item{rows}{Character vector. Columns to use as row headers (vertical axis).}

\item{cols}{Character vector. Columns to pivot into column headers (horizontal axis).
If empty and multiple measures selected, measures become columns instead.}

\item{measures}{Character vector. Numeric columns to aggregate. Multiple measures
are supported - if no column dimension is set, measures become columns.}

\item{agg_fun}{Character. Aggregation function: "sum", "mean", "median", "min", "max", "n".}

\item{digits}{Character or integer. Number of decimal places to round numeric results.
Empty string "" (default) means no rounding.}

\item{...}{Forwarded to \code{\link[blockr.core:new_transform_block]{blockr.core::new_transform_block()}}}
}
\value{
A blockr transform block that returns a pivoted data frame
}
\description{
A flexible pivot table block for slice & dice analysis. Map columns to
rows, columns, or leave them to be aggregated over. Inspired by Excel pivot
tables and dc.js-style dimensional analysis.
}
\details{
The pivot table block provides a flexible way to reshape and aggregate data:
\itemize{
\item \strong{Rows}: Dimensions that become row labels (like Region, Country)
\item \strong{Columns}: Dimension to pivot into columns (like Category, Year)
\item \strong{Measures}: Numeric values to aggregate (Revenue, Profit, etc.)
\item \strong{Aggregation}: How to combine values (sum, mean, etc.)
}

Dimensions not placed in rows or columns are automatically aggregated over.

\strong{Multiple measures behavior:}
\itemize{
\item No column dimension + 1 measure: Simple grouped table
\item No column dimension + N measures: Measures become columns
\item Column dimension + 1 measure: Dimension values become columns
\item Column dimension + N measures: Nested columns (Dimension_Measure)
}

The UI uses the shared \code{Blockr.Select} widget library (also used by
\code{blockr.dplyr} blocks) with a gear-icon popover for rounding precision.
}
\examples{
if (interactive()) {
  library(blockr.core)
  library(blockr.bi)

  # Single measure pivoted by dimension
  serve(
    new_pivot_table_block(
      rows = "Region",
      cols = "Category",
      measures = "Revenue"
    ),
    data = list(data = bi_demo_data())
  )

  # Multiple measures as columns (no column dimension)
  serve(
    new_pivot_table_block(
      rows = "Region",
      measures = c("Revenue", "Profit", "Quantity")
    ),
    data = list(data = bi_demo_data())
  )
}
}
