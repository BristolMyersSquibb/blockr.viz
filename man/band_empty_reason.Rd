% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/band-series.R
\name{band_empty_reason}
\alias{band_empty_reason}
\title{Why a distribution band came back empty}
\usage{
band_empty_reason(data, x_col, y_col, min_n = 12, id_col = NULL)
}
\arguments{
\item{data}{Data frame.}

\item{x_col}{Numeric x column (e.g. study day).}

\item{y_col}{Numeric value column.}

\item{min_n}{Minimum distinct subjects in a window; below it the band is
cut.}

\item{id_col}{Subject id column, counted DISTINCT for \code{n} and for the
adaptive window. \code{NULL} counts rows, which over-counts a cohort whenever
one subject contributes several observations to the same window.}
}
\value{
A one-sentence explanation, or \code{NULL} when the required mappings
are simply unset (the renderer's own role gate covers that).
}
\description{
\code{\link[=compute_band_series]{compute_band_series()}} returns \code{NULL} for a whole family of ordinary
reasons -- a non-numeric timeline, a cohort too thin to clear \code{min_n}, a
filter that emptied the frame. The renderer cannot tell them apart, so
without this it draws an empty panel and the reader is left guessing. Call
it only when the band IS \code{NULL}; it re-walks the same guards in order and
names the first one that fires.
}
\keyword{internal}
