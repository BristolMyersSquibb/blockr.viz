% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html-table.R
\name{html_table}
\alias{html_table}
\title{HTML Table Renderer for the Table-Blocks Quartet}
\usage{
html_table(
  data,
  title = NULL,
  caption = NULL,
  default_expanded = TRUE,
  max_height = "600px"
)
}
\arguments{
\item{data}{A wide tibble following the dotted-column contract.}

\item{title}{Optional table title (rendered as a \verb{<caption>}).}

\item{caption}{Optional trailing caption / footnote.}

\item{default_expanded}{Logical. When \code{TRUE} (default), all
sections start expanded. When \code{FALSE}, all level-1 sections start
collapsed and users click to reveal their contents.}

\item{max_height}{CSS max-height of the scroll container. Default
\code{"600px"}. Set to \code{NULL} to disable scrolling.}
}
\value{
A \code{\link[htmltools:tagList]{htmltools::tagList()}} containing the scoped style, the
\verb{<table>} element, and the initialisation script. Drop into any
\code{shiny::renderUI()} / \code{shiny::htmlOutput()} slot.
}
\description{
Renders the output of \code{\link[=summary_table]{summary_table()}} (pivoted display grids are
produced upstream by composing \code{summarize} with \code{tidyr::pivot_wider})
as a hand-rolled HTML table with nested row-side section headers,
multi-level column spanners, and client-side collapse/expand of
sections. Designed as a dashboard-native alternative to
\code{\link[=gt_table]{gt_table()}} -- static gt is ideal for print / CSR output, while
this renderer is tuned for interactive dashboards.
}
\details{
The input contract is the same dotted-column wide tibble that
\code{\link[=summary_table]{summary_table()}} produces (annotated-df v2):
\itemize{
\item \verb{.group1/.group1_level, ..., .group<k>/.group<k>_level} -- nested
row-side grouping pairs (variable name / value), outermost first.
Section headers are synthesized from value runs;
\verb{attr(.group<k>_level, "label")} carries the display prefix.
\item \code{.variable} / \code{.variable_level} / \code{.variable_label} -- leaf source
identity; runs of \code{.variable_label} render as the innermost
(variable) header blocks.
\item \code{.strong} / \code{.indent} -- fallback positional dialect for coerced
free-form tables (bold header rows + indented children).
\item \code{.label} -- innermost per-row identifier (stat name / factor
level). Rendered as the leftmost row-stub column.
\item Data columns -- names use \code{|} as nesting delimiter for multi-level
column spanners (e.g. \code{"KarXT|Week 2"}). \code{attr(col, "label")} may
carry HTML-wrapped display text for the leaf column header.
\item Cells are pre-formatted character strings.
}

v1 deliberately supports only the new wide format. Legacy
long-format input from \code{tidy_summary_block} is routed through
\code{\link[=gt_table]{gt_table()}} instead.
}
\examples{
tbl <- summary_table(iris, vars = "Sepal.Length", by = "Species")
html_table(tbl)
}
